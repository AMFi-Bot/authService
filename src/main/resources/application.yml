spring:
  data:
    mongodb:
      uri: ${AUTH_SERVICE_MONGO_URI}
  #! Development only
  devtools:
    remote:
      secret: ${DEVTOOLS_REMOTE_SECRET}
    restart:
      poll-interval: 3s
      quiet-period: 2s
